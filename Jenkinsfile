pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'start build!'
                sh 'python --version'
            }
        }
        stage('Test') {
            steps {
                echo 'start test!'
            }
        }
    }
}
